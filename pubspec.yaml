name: music
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  http: ^1.0.0
  flutter:
    sdk: flutter
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  shared_preferences: ^2.0.16
  crypto: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - img/
    - json/
    - assets/
    # - img/menu.png
    # - img/pic1.jpg
    # - img
